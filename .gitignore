.Rproj.user
.Rhistory
.RData
.Ruserdata
docs/
inst/doc
*.parquet
*.rds
cache/

# ...except the package's own reference data. `get_games_medals()` and
# `summary_games_economic_dominance()` read these through `system.file()`, so
# without them an installed copy of the package falls back to a hardcoded
# C:/dev path and fails anywhere but this machine. They are small (60KB and
# 290KB) and regenerated by citiusdata/scripts/harvest_*.R.
!inst/extdata/multisport_medal_tables.rds
!inst/extdata/country_economic_history.rds
