% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_athletics_calendar.R
\name{athletics_calendar_all}
\alias{athletics_calendar_all}
\title{Fetch the FULL World Athletics calendar for a query, paging automatically}
\usage{
athletics_calendar_all(
  start_date = NULL,
  end_date = NULL,
  query = NULL,
  max_pages = 500L,
  throttle = 1
)
}
\arguments{
\item{start_date, end_date}{\code{Date} or \code{"YYYY-MM-DD"} string, inclusive.
\code{NULL} for no bound on that side. \strong{The endpoint's own filter is not
strictly enforced} -- confirmed 2026-08-30, a 2015-only query returned
at least one 2007 result. It does most of the work; a caller needing a
hard boundary must post-filter \code{start_date} on the returned table.}

\item{query}{Free-text competition name search (substring/fuzzy, as the
site's own search box does), or \code{NULL}.}

\item{max_pages}{Safety cap on the number of pages fetched, in case
\code{hits} is corrupt or the loop otherwise fails to terminate. Defaults to
500 (50,000 rows), comfortably above the 2026-08-30 measured total.}

\item{throttle}{Seconds to sleep between page requests, additional to
\code{\link[=citius_get_html]{citius_get_html()}}'s own per-host throttle -- this is a real public
website, not a bulk data endpoint, and pulling ~400 pages back-to-back
with no pacing is not a reasonable way to use it.}
}
\value{
A single combined \code{data.table}, same columns as
\code{\link[=athletics_calendar]{athletics_calendar()}}. Attribute \code{"hits"} gives the total the server
reported. Attribute \code{"complete"} is \code{TRUE} only if every page fetched
cleanly (\code{fetch_ok}) AND the final row count matches \code{hits} (after
dropping unresolvable-id rows, see below) -- \strong{always check this before
trusting the result is the whole calendar}, since a page can fail
mid-run (a transient block, a WAF interstitial) without raising an
error, per \code{\link[=athletics_calendar]{athletics_calendar()}}'s \code{fetch_ok}. On \code{FALSE}, attribute
\code{"failed_offsets"} lists which page offsets did not fetch cleanly, and
\code{"dropped_bad_id"} gives how many rows were dropped for having no
parseable \code{competition_id} (found in review 2026-08-30: these used to
be silently merged into one \code{NA}-keyed group by the de-duplication
step, discarding every other genuinely distinct competition an
unparseable id happened to share \code{NA} with).
}
\description{
Loops \code{\link[=athletics_calendar]{athletics_calendar()}} over \code{offset} (steps of 100, the endpoint's
fixed page size) until every matching row has been fetched. For the
unfiltered world query that is ~393 requests (39,290 hits / 100) --
expect this to take real time, and narrow with \code{start_date}/\code{end_date}
when you don't actually need the whole history in one call.
}
\examples{
\dontrun{
athletics_calendar_all(start_date = "2026-01-01", end_date = "2026-12-31")
}
}
