% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_swimcloud.R
\name{swimcloud_fetch}
\alias{swimcloud_fetch}
\title{Fetch SwimCloud pages through an open browser session}
\usage{
swimcloud_fetch(sess, paths, concurrency = 6L, retries = 3L)
}
\arguments{
\item{sess}{A session from \code{\link[=swimcloud_session]{swimcloud_session()}}.}

\item{paths}{Character vector of site-relative paths.}

\item{concurrency}{Simultaneous requests. Do not raise above 6 without
re-measuring the 429 threshold.}

\item{retries}{Attempts for pages that come back non-200, with backoff.}
}
\value{
Character vector of HTML, same length and order as \code{paths}.
}
\description{
Pages are retrieved with an in-page \code{fetch()} rather than by navigating.
Results are server-rendered, so the fetched HTML is complete, and it skips
the render, images, fonts and scripts a navigation would pay for — measured
at 97ms per page against seconds for a navigation.
}
\details{
\strong{Concurrency is capped deliberately.} Measured: 6 concurrent requests
return 14/14 pages; \strong{10 concurrent returns HTTP 429 and silently loses 29\%
of the bytes} while looking like the fastest setting. This function
therefore verifies every response and errors rather than returning a short
result — a harvest that quietly drops pages is worse than one that stops.
}
