% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ability.R
\name{condition_prior}
\alias{condition_prior}
\title{Re-shrink ability toward the field rather than the whole event}
\usage{
condition_prior(ability, field = NULL, weight = 1)
}
\arguments{
\item{ability}{An ability table from \code{\link[=estimate_ability]{estimate_ability()}}, carrying
\code{ability_raw}, \code{shrinkage} and \code{prior_mu}.}

\item{field}{Optional character vector of \code{athlete_id}s defining the
population to shrink toward. Defaults to every athlete in \code{ability}, which
is a no-op — pass the entrants of the race being predicted.}

\item{weight}{How far to move from the unconditional prior to the field
prior, in \verb{[0, 1]}. \code{1} shrinks fully toward the field.}
}
\value{
\code{ability} with \code{ability}, \code{prior_mu} and \code{ability_se} updated.
}
\description{
Empirical Bayes shrinks a thinly-evidenced athlete toward \code{prior_mu}, the
UNCONDITIONAL mean ability in the event — computed across everyone rated,
including a long tail of club athletes who will never contest a final. The
athletes actually entered in a championship are a selected subset well above
that mean, so shrinking them toward it drags them below their true level, and
the more they are shrunk the worse it gets.
}
\details{
Measured on the athletics backtest: a finalist sits a median \strong{+1.36\%} above
the unconditional event mean (800m W +3.32\%, Long Jump M +2.91\%), and the
predicted-mark bias runs from −0.07\% for barely-shrunk athletes to \strong{−2.18\%}
for those shrunk more than 60\%.

The prior enters the shrinkage linearly, so re-conditioning is exact and needs
no refit:
\deqn{ability_{new} = ability_{old} + shrinkage 	imes (\mu_{new} - \mu_{old})}

The prior is built from \code{ability_raw}, never from the shrunk \code{ability}, which
would be circular — shrinking toward a mean that is itself the result of
shrinking compounds toward the centre with every pass.
}
\seealso{
\code{\link[=estimate_ability]{estimate_ability()}}
}
