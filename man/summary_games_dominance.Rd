% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/games.R
\name{summary_games_dominance}
\alias{summary_games_dominance}
\title{Summarize Single-Nation Multi-Sport Gold Dominance (Master Wrapper)}
\usage{
summary_games_dominance(
  games = NULL,
  top_n = 20L,
  min_golds = 5L,
  method = "raw",
  gamma = NULL
)
}
\arguments{
\item{games}{Optional character vector filtering by games (e.g. \code{"commonwealth"}, \code{"olympics_summer"}).}

\item{top_n}{Number of top performances to return. Defaults to 20.}

\item{min_golds}{Minimum golds required to qualify. Defaults to 5.}

\item{method}{Ranking method to use. Defaults to \code{"raw"}.}

\item{gamma}{Elasticity exponent for sub-linear log/power scaling. Defaults to empirical baseline (0.35 for pop, 0.45 for GDP).}
}
\value{
A \code{data.table} of dominance rankings.
}
\description{
Unified master wrapper function to evaluate historical multi-sport gold dominance across
raw gold shares, field size (competing nations), economic adjustments (GDP / Population),
and logistic log-odds scale metrics.
}
\details{
Supported \code{method} parameter options:
\itemize{
\item \code{"raw"} (default): Rank strictly by raw actual gold percentage won (\code{gold_pct}).
\item \code{"excess_nations"}: Rank by excess gold \% over uniform nation share (Actual \% - 100\% / competing_nations).
\item \code{"multiplier_nations"}: Rank by gold multiplier over uniform nation share (Actual \% / expected_pct).
\item \code{"logit_nations"}: Rank by logistic log-odds shift over uniform nation share.
\item \code{"excess_gdp"}: Rank by excess gold \% over GDP share (World Bank data).
\item \code{"excess_pop"}: Rank by excess gold \% over Population share (World Bank data).
\item \code{"multiplier_gdp"}: Rank by economic gold efficiency multiplier (Actual \% / GDP Share \%).
\item \code{"multiplier_pop"}: Rank by per-capita gold efficiency multiplier (Actual \% / Population Share \%).
\item \code{"logit_gdp"}: Rank by logistic log-odds shift over GDP share.
\item \code{"logit_pop"}: Rank by logistic log-odds shift over Population share.
}
}
