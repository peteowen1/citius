% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aging.R
\name{project_ability}
\alias{project_ability}
\title{Project an ability estimate to a different age}
\usage{
project_ability(ability, aging, max_shift = 0.05)
}
\arguments{
\item{ability}{A \code{data.table} from \code{\link[=estimate_ability]{estimate_ability()}} with an \code{age_now}
column giving each athlete's age at the target date, and \code{age_ref} giving
the mean age of the results the estimate was built from.}

\item{aging}{A \code{citius_aging} from \code{\link[=fit_aging_curve]{fit_aging_curve()}}.}

\item{max_shift}{Shift size, on the log performance scale, beyond which a
projection is reported as suspicious. Not a clamp — the shift is still
applied — but a warning names the usual cause (an \code{age_ref} that is a
career mean rather than the weighted mean from \code{\link[=estimate_ability]{estimate_ability()}}).}
}
\value{
\code{ability} with \code{ability} shifted by the age difference and an
\code{age_shift} column recording the adjustment applied.
}
\description{
Ability estimated from a career history reflects the ages at which those
performances happened. Predicting a Games two years out means asking what the
athlete will be capable of at their age \emph{then}, which for a 22-year-old and a
34-year-old are movements in opposite directions.
}
