% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{citius_events}
\alias{citius_events}
\title{Canonical event registry}
\usage{
citius_events()
}
\value{
A \code{data.table} with one row per canonical event.
}
\description{
The single source of truth for what an event \emph{is}: its units, which
direction counts as better, and the structural facts that change how it must
be modelled. Every source-specific event name is normalised onto this
registry by \code{\link[=match_event]{match_event()}}, so adding a new data feed never requires
touching the models.
}
\details{
Four columns carry real modelling weight and are worth understanding:

\describe{
\item{\code{orientation}}{\code{-1} where lower is better (all times), \code{+1} where
higher is better (jumps, throws, points). Consumed by \code{\link[=to_perf]{to_perf()}}.}
\item{\code{tactical}}{\code{TRUE} where championship finals are routinely run as
sit-and-kick races, so the winning \emph{time} is uninformative about ability.
Variance estimated from these races is inflated and must be handled
separately — see \code{\link[=estimate_variance]{estimate_variance()}}. Empirically this is severe:
an elite 1500m runner's raw career spread is roughly 4.5 percent, but
most of that is tactics, not form.}
\item{\code{technical}}{\code{TRUE} for events with a discrete failure mode (fouls,
no-height) that is \emph{not} a draw from the continuous performance
distribution and must be simulated as a separate Bernoulli process.}
\item{\code{cv_prior}}{Prior on within-athlete coefficient of variation, on the
log scale. Used to shrink noisy per-athlete variance estimates. Swimming
values are markedly lower than athletics because the pool removes wind
and, largely, tactics.}
}
}
\examples{
ev <- citius_events()
ev[ev$sport == "Swimming" & ev$sex == "W", c("event_id", "cv_prior")]
}
