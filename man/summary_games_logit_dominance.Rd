% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/games.R
\name{summary_games_logit_dominance}
\alias{summary_games_logit_dominance}
\title{Summarize Single-Nation Gold Dominance by Logistic Coefficient (Logit Shift & Odds Ratio)}
\usage{
summary_games_logit_dominance(games = NULL, top_n = 20L, min_golds = 5L)
}
\arguments{
\item{games}{Optional character vector filtering by games.}

\item{top_n}{Number of top performances to return. Defaults to 20.}

\item{min_golds}{Minimum golds required to qualify. Defaults to 5.}
}
\value{
A \code{data.table} sorted by \code{logit_shift} descending.
}
\description{
Evaluates gold dominance on the logistic log-odds scale:
\enumerate{
\item \code{p_actual} = \code{golds / total_event_golds}
\item \code{p_expected} = \code{1 / competing_nations}
\item \code{logit(p)} = \code{log(p / (1 - p))}
\item \code{logit_shift} = \code{logit(p_actual) - logit(p_expected)}
\item \code{odds_ratio} = \code{exp(logit_shift)}
}
}
