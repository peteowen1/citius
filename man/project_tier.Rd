% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{project_tier}
\alias{project_tier}
\title{Put an ability estimate onto the tier of the race being predicted}
\usage{
project_tier(ability, tier, calibration = NULL, shrink = 0.5)
}
\arguments{
\item{ability}{Ability rows, as returned by \code{\link[=estimate_ability]{estimate_ability()}}.}

\item{tier}{Tier code for the race being predicted, recycled to \code{ability}.
Raw feed codes are accepted and classified by the same rule the calibration
was fitted under.}

\item{calibration}{A \code{citius_calibration} carrying a \verb{$tier} table. \code{NULL}
returns \code{ability} unchanged, which is the honest fallback: without measured
offsets there is nothing to add back.}

\item{shrink}{Fraction of the measured offset to apply. \strong{0.5 by default, and
the default is measured, not chosen.} The offsets are fitted on ALL
within-athlete history, while the races actually being predicted are a
selected subset -- good enough to attract elite fields, and therefore faster
than the average meet at their tier. Applying them in full overshoots: low
tier goes from -0.98\% bias to +0.71\%.

Fitted on backtest finals before 2023 and evaluated on those after:\tabular{lll}{
   lambda \tab test MAE \tab test bias \cr
   0.0 \tab 2.3173 \tab -0.460 \cr
   0.4 \tab 2.3015 \tab -0.241 \cr
   \strong{0.5} \tab \strong{2.3021} \tab \strong{-0.186} \cr
   1.0 \tab 2.3297 \tab +0.089 \cr
   1.3 \tab 2.3658 \tab +0.255 \cr
}


An interior optimum bracketed on both sides. Note that zeroing the bias
(lambda 1.3) makes MAE clearly worse -- the two objectives conflict, and
MAE is the one with a threshold attached. Fitting a separate offset per
tier was also tried and OVERFITS: it reversed the bias out of sample
(-0.460 to +0.116) and worsened MAE, because low-tier bias is not stable
across eras (train -0.570 against a test reality of -1.19).}
}
\value{
\code{ability} with \code{ability} shifted and \code{tier_adj} recording the shift.
}
\description{
\code{\link[=estimate_ability]{estimate_ability()}} with \code{adjust_context = TRUE} SUBTRACTS the round and tier
offsets from every historical mark, so the ability it returns describes a
final at the TOP tier. \code{\link[=simulate_event]{simulate_event()}} then predicts a mark straight from
that number. Nothing puts the tier back, so every race — a club meet, a
Diamond League, a Games final — is predicted as though it were a top-tier
final.
}
\details{
Measured on 3,696 backtest finals (2026-07-31), signed so that negative means
athletes ran WORSE than predicted:\tabular{llll}{
   event \tab champs \tab pro \tab lower \cr
   5000m M \tab +0.25 \tab -0.80 \tab \strong{-2.39} \cr
   1500m M \tab +0.53 \tab -0.15 \tab -1.59 \cr
   800m M \tab +0.23 \tab -0.06 \tab -0.46 \cr
}


Championship races are the reference and are unbiased; everything below is
predicted too fast. Fitted jointly, tier explains more of the error than race
distance does (R2 0.093 against 0.054).

This is the exact counterpart of \code{\link[=project_championship]{project_championship()}}, which exists
because the championship half of the same correction "has to go back on --
without this half the correction runs one way and makes predictions worse".
The tier half was never written.

\strong{Not applied by default.} It changes every non-top-tier prediction, so it
belongs in its own backtest arm rather than riding along with another change.
}
\seealso{
\code{\link[=project_championship]{project_championship()}}, \code{\link[=estimate_ability]{estimate_ability()}}
}
