% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{stage_probs}
\alias{stage_probs}
\title{Medal probabilities from each vantage point in a championship}
\usage{
stage_probs(
  ability,
  fields,
  structure = NULL,
  n_sims = 10000L,
  calibration = NULL,
  seed = NULL
)
}
\arguments{
\item{ability}{Ability estimates covering every athlete in \code{fields}.}

\item{fields}{Named list of athlete_id vectors, one per stage, in order.}

\item{structure}{Round structure for stages where qualification is still
pending, as taken by \code{\link[=simulate_rounds]{simulate_rounds()}}. \code{NULL} means a single race.}

\item{n_sims}{Simulations per stage.}

\item{calibration}{Optional \code{citius_calibration}.}

\item{seed}{Optional integer seed.}
}
\value{
A \code{data.table} with one row per athlete per stage: \code{stage},
\code{athlete_id}, \code{p_gold}, \code{p_medal}, \code{p_final}, plus \code{delta_gold} against the
previous stage.
}
\description{
A single medal probability answers "who wins?" without saying \emph{when it was
asked}. The honest answer changes as the meet progresses, and the change is
itself the interesting part: an athlete at 30\% before the heats who is 55\%
after them has told you something, and one who is eliminated has told you
more.
}
\details{
Three vantage points, each answering a different question:

\describe{
\item{\code{entry}}{Everyone entered. "Who wins if they all turn up?" —
including qualification uncertainty for every round.}
\item{\code{contested}}{Everyone who actually started a round. Removes
withdrawals, which are a field error rather than a model error.}
\item{\code{final}}{The known finalists. Pure head-to-head, no qualification
risk left.}
}

Comparing \code{entry} with \code{contested} isolates how much a forecast was damaged
by withdrawals; comparing \code{contested} with \code{final} isolates how much was
decided by qualification rather than by the final itself.
}
\examples{
\dontrun{
stage_probs(ability,
            fields = list(entry = entered, contested = started, final = finalists),
            structure = list(list(races = 3, advance = 2, fastest_losers = 2),
                             list(races = 1)))
}
}
