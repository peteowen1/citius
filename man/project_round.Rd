% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{project_round}
\alias{project_round}
\title{Put an ability estimate onto the round of the race being predicted}
\usage{
project_round(ability, round, calibration = NULL, shrink = 1)
}
\arguments{
\item{ability}{Ability rows, as returned by \code{\link[=estimate_ability]{estimate_ability()}}.}

\item{round}{Round label for the race being predicted (raw feed labels
accepted -- classified by the same internal rule the calibration was
fitted under), recycled to \code{ability}.}

\item{calibration}{A \code{citius_calibration} carrying a \verb{$round} table. \code{NULL}
returns \code{ability} unchanged, which is the honest fallback: without
measured offsets there is nothing to add back.}

\item{shrink}{Fraction of the measured offset to apply. Unmeasured for
round (see above) -- defaults to 1, the full raw offset.}
}
\value{
\code{ability} with \code{ability} shifted and \code{round_adj} recording the
shift.
}
\description{
\code{\link[=estimate_ability]{estimate_ability()}} with \code{adjust_context = TRUE} SUBTRACTS the round offset
from every historical mark, bringing history to a FINAL footing. Nothing
puts the round back for the race being predicted -- \code{simulate.R} has no
round logic at all -- so a heat is forecast exactly like a final. This is
\code{\link[=project_tier]{project_tier()}}'s missing counterpart: the tier half of "the correction
runs one way" was fixed here; the round half never was (see
\code{docs/reference/modelling-traps.md}, "The correction runs one way").
}
\details{
\strong{Unlike \code{\link[=project_tier]{project_tier()}}, no shrink factor has been measured for round.}
\code{project_tier()}'s \code{shrink = 0.5} default came from a lambda sweep on
backtest finals split before/after 2023 (see its own docs) -- fitted
offsets overshoot because they are measured on all history while the races
actually predicted are a faster, selected subset. No equivalent sweep has
been run for round, so \code{shrink = 1} here is the honest, unshrunk default
(the raw measured offset applied in full), not a validated choice. Run the
same before/after backtest split before trusting this in a forecast that
matters, and update the default once that number exists.

\strong{Not applied by default.} Changes every non-final prediction, so it
belongs in its own backtest arm rather than riding along with another
change -- same reasoning as \code{project_tier()}.
}
\seealso{
\code{\link[=project_tier]{project_tier()}}, \code{\link[=project_championship]{project_championship()}}, \code{\link[=estimate_ability]{estimate_ability()}}
}
