% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{read_results_store}
\alias{read_results_store}
\title{Read from a partitioned result store}
\usage{
read_results_store(path, events = NULL, from = NULL, to = NULL, columns = NULL)
}
\arguments{
\item{path}{Store directory written by \code{\link[=write_results_store]{write_results_store()}}.}

\item{events}{Event ids to read, or \code{NULL} for all.}

\item{from, to}{Inclusive date bounds as \code{Date}, or \code{NULL}.}

\item{columns}{Columns to return, or \code{NULL} for all.}
}
\value{
A \code{data.table}.
}
\description{
Filters and column selection are pushed down to the files, so a query touches
only the partitions it needs and never materialises the rest.
}
\section{Dates}{

Pass real \code{Date} objects. Arrow cannot subtract a number from a timestamp, so
\code{date >= cut - 4380} fails inside a query with
\code{NotImplemented: subtract_checked} — compute the bound in R first. That is why
this takes \code{from}/\code{to} rather than a filter expression.
}

\examples{
\dontrun{
read_results_store("citiusdata/data/athletics_store",
                   events = "AT-100Metres-M",
                   from = as.Date("2016-01-01"), to = as.Date("2024-08-01"))
}
}
