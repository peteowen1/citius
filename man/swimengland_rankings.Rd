% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_swimengland.R
\name{swimengland_rankings}
\alias{swimengland_rankings}
\title{Fetch one page of Swim England event rankings}
\usage{
swimengland_rankings(
  stroke,
  pool = "L",
  sex = "M",
  year = "A",
  nationality = "A",
  start = 1L,
  n = 100L
)
}
\arguments{
\item{stroke}{Stroke code, see \code{\link[=swimengland_strokes]{swimengland_strokes()}}.}

\item{pool}{\code{"L"} long course or \code{"S"} short course. \strong{Never pool the two} —
short course is roughly 5\% faster, which is several times the measured
within-athlete spread.}

\item{sex}{\code{"M"} or \code{"F"}.}

\item{year}{Four-digit year, or \code{"A"} for all-time.}

\item{nationality}{One of \code{A} British, \code{E} England, \code{S} Scotland, \code{W} Wales,
\code{J} Jersey, \code{G} Guernsey, \code{I} Isle of Man, \code{X} all members.}

\item{start}{First rank to return (1-based); the feed pages from here.}

\item{n}{Rows per request. The server caps this at 100 however much is asked
for, so larger values silently do nothing.}
}
\value{
A \code{data.table} in the canonical result schema, plus \code{tiref} (the
stable Swim England swimmer id, which makes cross-year linking exact rather
than name-based).
}
\description{
Returns the ranked list for one event, pool, sex, year and nationality. Each
row is a swimmer's \strong{best} time for that combination, so a year-by-year
sweep yields a season-best series per athlete rather than a single number.
}
\details{
\strong{These are bests, not full histories.} Ranked lists are truncated at the
good end by construction, so they support ability estimation but \strong{not}
variance estimation — see the note in \code{estimate_ability()} about top-lists.
The returned rows carry \code{is_best = TRUE} so a downstream calibration can
exclude them rather than silently treating them as ordinary results.
}
