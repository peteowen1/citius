% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/positions.R, R/positions_forecast.R
\name{position_probs}
\alias{position_probs}
\title{Full finishing-position distribution}
\usage{
position_probs(sim, k = 8L)

position_probs(sim, k = 8L)
}
\arguments{
\item{sim}{A \code{citius_sim} from \code{\link[=simulate_event]{simulate_event()}}.}

\item{k}{Highest finishing position to report. Positions beyond the field
size are still emitted, as zeros, so every race in a store has the same
columns and they can be bound without a fill rule.}

\item{max_position}{Highest position to report individually. Positions beyond
it are pooled into one \code{NA} row per athlete so the probabilities still sum
to 1.}

\item{wide}{Return one column per position rather than long format.}
}
\value{
A \code{data.table}. Long (default): \code{athlete_id}, \code{position}, \code{prob},
\code{cum_prob}. Wide: \code{athlete_id} then \code{pos_1}, \code{pos_2}, ...

A \code{data.table} with \code{athlete_id} and \code{p_pos_1} .. \code{p_pos_k}, ordered
by \code{p_pos_1} descending. Rows sum to at most 1; the shortfall is the
probability of finishing worse than \code{k}.
}
\description{
\code{\link[=medal_probs]{medal_probs()}} collapses the simulation to gold and medal probability, which
throws away most of what was simulated. The rank matrix already holds the
whole distribution: how often each athlete finished 1st, 2nd, 4th, last.

\code{\link[=medal_probs]{medal_probs()}} reduces \code{sim$rank} to three summaries -- \code{p_gold},
\code{p_medal}, \code{p_top8} -- and the rest of the distribution is discarded with
the simulation object. This keeps the whole top-\code{k} instead, one column per
finishing position, so "what were his odds of finishing 4th" is answerable
later without re-simulating.
}
\details{
Fourth place is the obvious use — the cruellest position in the sport and one
no medal-probability summary can express — but the distribution also carries
the shape of a forecast. Two athletes can share a 15\% medal chance while one
is a likely 4th with upside and the other is a likely 8th who occasionally
produces something extraordinary. Those are different athletes to back.

\strong{Wide, capped at \code{k}, deliberately.} A long form (one row per athlete per
position) scales with field size SQUARED per race: measured on the corpus
2026-09-14, the largest single race is an 816-starter marathon, which is 816
rows wide against \strong{665,856} long. Across every 2026 race it is 1,032,553
rows wide against 13,016,281 long. Capping at \code{k} makes the row count a
fixed one-per-athlete-per-race whatever the field size, which is the whole
point -- field sizes run from a median of 4 to a maximum of 944.

\code{k = 8} is the domain's own boundary rather than an arbitrary one: athletics
scores points to eighth place. Nothing is lost by the cap, because the
probability of finishing \code{k}-th or worse is \verb{1 - sum(p_pos_1 .. p_pos_k)}.
}
\examples{
\dontrun{
sim <- simulate_event(entrants, n_sims = 10000)
position_probs(sim)
position_probs(sim, wide = TRUE)
}
\dontrun{
sim <- simulate_event(proj, n_sims = 20000)
position_probs(sim)          # p_pos_1 .. p_pos_8
}
}
\seealso{
\code{\link[=medal_probs]{medal_probs()}}, which this complements rather than replaces.
}
