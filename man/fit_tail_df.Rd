% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{fit_tail_df}
\alias{fit_tail_df}
\title{Estimate how heavy the performance tails are}
\usage{
fit_tail_df(
  results,
  candidates = c(4, 5, 6, 8, 10, 15, 20, 30, 50, Inf),
  probes = c(1.5, 2, 2.5, 3),
  clip = 8
)
}
\arguments{
\item{results}{Canonical results with a \code{race_key}, or a decomposition from
\code{\link[=decompose_races]{decompose_races()}}.}

\item{candidates}{Degrees of freedom to evaluate; \code{Inf} represents a normal.}

\item{probes}{Deviation sizes, in standard deviations, to match on.}

\item{clip}{Standardised residuals beyond this are treated as data errors and
excluded. They are far outside any plausible performance and exist to stop
feed corruption driving the fit.}
}
\value{
A \code{data.table} of \code{df} and fit error, best first.
}
\description{
Fits the degrees of freedom of the scaled-t distribution that
\code{\link[=simulate_event]{simulate_event()}} draws performances from, by matching the \emph{observed
probability of moderate deviations} rather than a moment.
}
\details{
Kurtosis is deliberately not used. Real result feeds contain a handful of
grossly wrong values, and the fourth moment is so dominated by them that
measured excess kurtosis reached 32 on championship data — implying \code{df}
around 4 when the actual tail mass sits only slightly above normal. Following
that would have made the simulator \emph{more} dispersed, worsening the
under-confidence it was meant to fix.

What matters for who wins a race is the shoulder of the distribution, not its
extremes, so \code{df} is chosen to minimise discrepancy in \verb{P(|z| > k)} across
moderate \code{k}. On championship athletics data the old hard-coded \code{df = 6} put
roughly three times too much mass beyond two standard deviations.
}
