% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_swimcloud.R
\name{swimcloud_event}
\alias{swimcloud_event}
\title{Translate a SwimCloud event name to the registry's discipline}
\usage{
swimcloud_event(x)
}
\arguments{
\item{x}{Character vector of SwimCloud event names.}
}
\value{
A list with \code{discipline} and \code{sex}, both \code{NA} where unrecognised.
}
\description{
SwimCloud writes US-style abbreviations — \code{"50 Breast Women"}, \code{"200 IM Men"}
— where \code{\link[=citius_events]{citius_events()}} uses \code{"50m Breaststroke"}. Without translation
\code{\link[=match_event]{match_event()}} returns \code{NA} for \strong{every} row, which is the correct
behaviour (it refuses to guess) but leaves the whole feed unusable.
}
\details{
The mapping is explicit rather than fuzzy, for the same reason \code{match_event()}
is strict: silently snapping an unrecognised event onto a neighbour corrupts
athlete histories in a way nothing downstream can detect.
}
\examples{
swimcloud_event("50 Breast Women")
swimcloud_event("200 IM Men")
}
