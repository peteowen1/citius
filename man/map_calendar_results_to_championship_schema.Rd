% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_athletics_calendar_results_map.R
\name{map_calendar_results_to_championship_schema}
\alias{map_calendar_results_to_championship_schema}
\title{Map athletics_calendar_results() output onto championship_results' schema}
\usage{
map_calendar_results_to_championship_schema(results)
}
\arguments{
\item{results}{Output of \code{\link[=athletics_calendar_results]{athletics_calendar_results()}}.}
}
\value{
A \code{data.table} with \code{CITIUS_DB_SCHEMA$championship_results}'s
column set (any column this source cannot supply is present as an
all-\code{NA} column, not omitted, so \code{rbindlist(fill = TRUE)} against the
existing corpus does not silently reorder columns).
}
\description{
\code{athletics_calendar_results()}'s own \verb{@section} documents real,
irreducible gaps against \code{CITIUS_DB_SCHEMA$championship_results}
(\code{db_schema.R}) -- this function does NOT fill those gaps, it maps what
is genuinely derivable and leaves the rest \code{NA}, matching the schema
guard's warn-only (not abort) policy for missing columns. Built to
backfill the 22 competitions found resolvable via the direct-results
path after the \code{worldathletics.nimarion.de} wrapper failed on them
(2026-08-30) -- see \code{docs/reference/harvesting.md} and this session's
history for context. This is a one-off backfill helper, not a new
standing harvest path.
}
\details{
Derivable, not left NA, because they are computable from what the source
DOES give (not fabricated):
\itemize{
\item \code{event_id} (canonical citius id) via \code{match_event()} on the raw
\code{event_name} + \code{sex_code} -- the source's OWN \code{event_id} is World
Athletics' internal id, a different namespace, kept only as an input to
\code{race_key} (mirroring \code{source_athletics.R}'s existing convention of
building \code{race_key} from the raw source id before \code{match_event()} ever
runs), never written to the output's \code{event_id} column.
\item \code{is_technical}, \code{orientation} via the matched event's row in
\code{citius_events()} -- registry properties, not per-result data.
\item \code{perf} via \code{to_perf(mark, orientation)}, once \code{mark} and \code{orientation}
are known.
\item \code{date} prefers \code{race_date}; where that is \code{NA} (common -- see the
fetch function's own docs) falls back to \code{comp_start}, which is always
populated. This is an approximation for a multi-day meet's later
rounds, not a defect -- callers needing exact per-round dates cannot
get them from this source at all.
\item \code{race_key}: \code{competition_id | wa_event_id | discipline | race_id | round | discriminator} (\code{round} is the response's own race label, e.g.
\code{"Heat 1"}/\code{"Final"} -- real supplied data, folded in unconditionally
since it costs nothing when already distinct and is exactly what
separates two races when \code{race_id} AND \code{race_number} both collide),
mirroring \code{source_athletics.R}'s existing pattern of
keying on the RAW source event id plus a race identifier plus the
discipline text (not \code{race_id} alone, which this endpoint's own docs
note is a small shared code across competitions, e.g. \code{"10"} for a
plain final -- not competition-unique on its own). The discriminator is
\code{race_number} ONLY where it actually distinguishes every race within
that (competition, event) -- non-\code{NA}, positive, not duplicated. Where
it doesn't (found live on this endpoint, not hypothetical: a sentinel
like \code{-1}/\code{1} repeated across an event's races), it falls back to a
content hash of the race's athlete ids, exactly mirroring
\code{source_athletics.R}'s \code{.race_discriminator()} -- reusing that internal
function directly would require it to operate on nested feed JSON this
already-flattened input doesn't have, so this re-implements the same
rule against a flat \code{data.table} instead. \strong{Trusting \code{race_number} raw
is exactly the bug that corrupted \code{condition_sd}/\code{sigma_within} in the
2026-07-27 Glasgow incident} (\verb{docs/incidents/ race-key-heat-collapse-2026-07-27.md}) -- silently, no test failed.
\strong{Residual, honestly-unfixable gap}: if \code{race_id}, \code{race_number} AND
\code{round} ALL collide for two genuinely different races in one event,
this function cannot separate them -- the flattened input has already
lost whatever distinguished them in the source's nested JSON (their
original array position), and no field survives to reconstruct it
from. Tested with a synthetic worst case 2026-08-30: confirmed this
collapses to one \code{race_key} rather than erroring. Not observed on real
data (round labels are effectively always distinct per real WA
competition); flagged as a known limit, not assumed impossible.
}

Left \code{NA}, genuinely absent from this source, not guessed:
\code{birthdate_year_only}, \code{sport} (set to the fixed \code{"Athletics"} literal,
matching \code{source_athletics.R}'s convention, not left \code{NA}),
\code{discipline_code}, \code{event_name} (this source has no separate age-division
qualifier field distinct from \code{discipline}), \code{comp_day}, \code{value_raw}
(no raw \code{performanceValue} integer, only the pre-formatted display
\code{mark} string), \code{indoor}, \code{legal}, \code{tier}, \code{comp_tier}, \code{age} (would need
a reliable per-result date, which \code{date} above is only an approximation
of), \code{venue_country}/\code{venue_city}/\code{venue_stadium} -- attempted via
best-effort parse of the single free-text \code{venue} field (pattern
\code{"<stadium>, <city> (<COUNTRY>)"}), left \code{NA} when the string doesn't
match that shape rather than guessing a split.
}
\examples{
\dontrun{
r <- athletics_calendar_results(7214476)
map_calendar_results_to_championship_schema(r)
}
}
