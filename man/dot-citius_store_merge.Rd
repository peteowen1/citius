% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_store.R
\name{.citius_store_merge}
\alias{.citius_store_merge}
\title{The one merge/replace engine every \verb{store_*()} wrapper calls into}
\usage{
.citius_store_merge(
  conn,
  table_name,
  new,
  dedup_key = "competition_id",
  schema = NULL,
  mode = c("merge", "replace")
)
}
\arguments{
\item{conn}{Writable connection.}

\item{table_name}{Target table.}

\item{new}{New data (data.frame or data.table).}

\item{dedup_key}{Column whole rows of which are deduplicated on, for
\code{mode = "merge"}. Ignored for \code{mode = "replace"}.}

\item{schema}{Expected column names, used only when the table does not yet
exist (see \code{CITIUS_DB_SCHEMA} in db_schema.R).}

\item{mode}{\code{"merge"} (default) or \code{"replace"} (full rebuild).}
}
\value{
Invisibly, the number of rows added (merge) or written (replace).
}
\description{
Schema guard (abort on an extra column the target lacks -- would be
silently dropped; warn-only on a missing column) modeled directly on
\code{bouncer::store_player_game_data()}. Merge dedup drops whole competitions
already present -- never row-level -- exactly
\code{citiusdata/scripts/merge_referenced.R}'s pattern, now inside a
transaction that rolls back on a failed row-count assertion instead of
leaving a partial or silently-wrong table.
}
\keyword{internal}
