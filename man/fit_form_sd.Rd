% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/form.R
\name{fit_form_sd}
\alias{fit_form_sd}
\title{Estimate irreducible form variation}
\usage{
fit_form_sd(
  results,
  calibration = NULL,
  half_life = 730,
  n_meets = 150L,
  min_history = 2000L
)
}
\arguments{
\item{results}{Canonical results with \code{perf}, \code{event_id}, \code{date} and
\code{competition_id}.}

\item{calibration}{A \code{citius_calibration}, used for the ability estimates.}

\item{half_life}{Recency half-life in days, matching prediction-time use.}

\item{n_meets}{Meets to sample across the period. More is slower and the
estimate is stable well below the default.}

\item{min_history}{Minimum prior rows before a meet is usable.}
}
\value{
A one-row \code{data.table} with \code{form_sd}, \code{n} residuals, and the
\code{sd_before} / \code{sd_after} standardised residual sd it achieves.
}
\description{
\code{sigma} measures how much an athlete varies around their own ability.
\code{ability_se} measures how little we know that ability, and shrinks toward
zero as evidence accumulates. Together they were the simulator's whole
predictive spread — and that spread was measurably too narrow.
}
\details{
Out-of-sample standardised residuals had sd \strong{1.55} rather than 1, and the
miss grew with evidence: sd 1.18 for athletes with \code{w_total < 0.5} against
1.74 for \code{w_total > 4}. That direction is the diagnostic. If \code{ability_se}
were merely understated, thinly-observed athletes would be worst. Instead the
\emph{best}-known athletes were worst, because \code{ability_se} drives their
uncertainty toward zero while their form on a given day still varies.

What is missing is variance that \strong{does not shrink with evidence}: taper,
illness, training phase, motivation on the day. This function measures it as
the excess of observed out-of-sample residual variance over what the model
claims:

\deqn{\tau^2 = \mathrm{var}(\mathrm{perf} - \mathrm{ability}) -
  \mathrm{mean}(\sigma^2 + \mathrm{se}^2)}

Held-out by construction: ability for each meet is estimated only from
results dated before it, so \code{tau} cannot absorb in-sample overfitting the way
a residual-variance estimate would.
}
\section{Why a constant, not a proportion}{

\code{sigma} ranges from 0.015 (sprint) to 0.061 (throw), so "constant" and
"proportional to each family's spread" are materially different designs.
Measured on 11k held-out residuals, a constant normalises sd(z) far better —
mean \eqn{|sd - 1|} of 0.062 across families against 0.297 — and it is
self-limiting in the right direction: adding 0.017 in quadrature nearly
doubles a sprinter's 0.015 spread, where over-confidence was worst
(raw sd 1.88), but moves a thrower's 0.061 by 4\%, where it was already fine
(raw sd 1.11). Throws and road running need no correction at all.
}

\examples{
\dontrun{
fit_form_sd(clean, calibration, half_life = 730)
}
}
