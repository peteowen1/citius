% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{conditions_params}
\alias{conditions_params}
\title{Load the exported conditions-model parameters for an event}
\usage{
conditions_params(event_id, dir)
}
\arguments{
\item{event_id}{Event id, e.g. \code{"AT-100Metres-M"}.}

\item{dir}{Directory of \verb{<event_id>.json} parameter files.}
}
\value{
A list, or \code{NULL} when the event has no fitted model.
}
\description{
Each event's \code{gamm4} conditions fit (wind, altitude, indoor, plus the
athlete / race-shock / residual variance components) is exported once per
refit to a small JSON file by \code{citiusdata/scripts/export_conditions_params.R}.
The curves are the fitted smooths evaluated on a grid, relative to the
reference point (wind 0, altitude 0, outdoor), in perf-log units. Nothing
here needs a model runtime: \code{\link[=adjust_conditions]{adjust_conditions()}} is linear interpolation
and \code{\link[=race_shock]{race_shock()}} is one line of arithmetic, so the same numbers can be
applied live (R or JS) as in a batch rebuild.
}
