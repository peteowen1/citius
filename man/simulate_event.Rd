% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_event}
\alias{simulate_event}
\title{Simulate an event and return outcome probabilities}
\usage{
simulate_event(
  ability,
  n_sims = 10000L,
  condition_sd = NULL,
  df = NULL,
  foul_prob = NULL,
  taper = 0,
  form_sd = NULL,
  calibration = NULL,
  seed = NULL
)
}
\arguments{
\item{ability}{A \code{data.table} from \code{\link[=estimate_ability]{estimate_ability()}}, filtered to the
entrants in this race. Must contain \code{athlete_id}, \code{event_id}, \code{ability}
and \code{sigma}.}

\item{n_sims}{Number of simulations.}

\item{condition_sd}{Shared-shock standard deviation. Defaults to
\code{\link[=race_conditions]{race_conditions()}} for the event.}

\item{df}{Degrees of freedom for the performance t distribution. Defaults to
the measured value from \code{calibration} (see \code{\link[=fit_tail_df]{fit_tail_df()}}), or 20.}

\item{foul_prob}{Probability that an athlete records no valid performance —
a foul-out or no-height in a field event, a DNF or DNS on the track.
Defaults to the measured rate from \code{calibration}.}

\item{taper}{Systematic shift applied to every athlete, on the log
performance scale. Positive values make the field faster; use this to
represent championship tapering.}

\item{form_sd}{Irreducible day-to-day form variation, on the log performance
scale. Unlike \code{ability_se} this does \strong{not} shrink as evidence
accumulates. Defaults to the measured value from \code{calibration}; see
\code{\link[=fit_form_sd]{fit_form_sd()}}.}

\item{calibration}{Optional \code{citius_calibration} from \code{\link[=calibrate]{calibrate()}}. Supplies
the measured shared-shock magnitude, per-athlete condition sensitivity and
foul rate. Without it the simulator falls back to registry placeholders.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An object of class \code{citius_sim}: a list with the raw \code{perf} matrix
(\code{n_sims} x athletes), \code{rank} matrix, the \code{ability} input and the settings
used.
}
\description{
Runs a Monte Carlo over a single race. Each simulation draws one shared
condition shock for the field, then one performance per athlete, then ranks
them. Repeating this many times converts point estimates of ability into the
distributional quantities that are actually interesting: medal chances,
finishing-position spreads and the odds of beating a given mark.
}
\details{
Athlete performances are drawn from a scaled t distribution rather than a
normal. Elite results have fatter tails than Gaussian noise implies — the
occasional breakthrough run and the occasional blow-up both happen more often
than a normal would allow — and a t with modest degrees of freedom captures
that without extra parameters.
}
\examples{
ab <- data.frame(
  athlete_id = c("a", "b", "c"),
  event_id = "AT-100Metres-M",
  ability = -log(c(9.8, 9.9, 10.0)),
  sigma = 0.008
)
sim <- simulate_event(ab, n_sims = 2000, seed = 1)
medal_probs(sim)
}
\seealso{
\code{\link[=medal_probs]{medal_probs()}}, \code{\link[=prob_better_than]{prob_better_than()}}
}
