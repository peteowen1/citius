% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{to_perf}
\alias{to_perf}
\title{Convert marks to the oriented performance scale}
\usage{
to_perf(mark, orientation)
}
\arguments{
\item{mark}{Numeric vector of parsed marks (see \code{\link[=parse_mark]{parse_mark()}}).}

\item{orientation}{Integer vector, \code{-1} where lower is better, \code{+1} where
higher is better. Recycled to the length of \code{mark}.}
}
\value{
Numeric vector on the performance scale; higher is better.
}
\description{
The single most important abstraction in the package. Athletics and swimming
mix events where \emph{lower is better} (all track and swim times) with events
where \emph{higher is better} (throws, jumps, combined-event points). Every
downstream model — ability estimation, variance, simulation, ranking — is
written against a scale where \strong{higher is always better}, so that no
function needs to branch on event type.
}
\details{
The transform is \code{perf = orientation * log(mark)}, with \code{orientation = -1}
for time events and \code{+1} for field events and points.

Working in logs (rather than raw units) is deliberate: performance spread is
close to multiplicative, so a fixed log-scale standard deviation means the
same \emph{percentage} spread across a 9.58s 100m and a 2:01:09 marathon. It also
enforces the physical constraint that times and distances are positive.
}
\examples{
# A faster 100m yields a higher performance score
to_perf(c(9.58, 10.20), orientation = -1)
}
\seealso{
\code{\link[=perf_to_mark]{perf_to_mark()}} for the inverse.
}
