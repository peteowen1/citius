% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{athlete_crosswalk}
\alias{athlete_crosswalk}
\title{Build a cross-source athlete crosswalk}
\usage{
athlete_crosswalk(x, name_order = NULL, links = NULL, fuzzy_scope = NULL)
}
\arguments{
\item{x}{A data frame of athletes with columns \code{source} and \code{athlete_name},
optionally \code{athlete_id}, \code{country}, \code{sport} and \code{birthdate}. One row per
athlete per source; duplicates are collapsed.}

\item{name_order}{Named character vector giving the fallback name order per
source, e.g. \code{c(worldaquatics = "surname_first", crs = "given_first")}.
Consulted only for names carrying no capitalisation signal — and it matters:
World Aquatics writes \code{"SHORT Samuel"} while World Athletics writes
\code{"Taoufik Makhloufi"}.}

\item{links}{Optional data frame of externally verified identities, with
\code{source}, \code{link_id}, and \code{athlete_id} and/or \code{athlete_name}. Rows sharing a
\code{link_id} are taken to be the same person and are merged before any key
pass, overriding the ambiguity guards. Use it for identities confirmed
outside the name keys — a resolver hit against a search endpoint, or a
correction made by hand. Without it, an athlete whose loose key is
ambiguous (\code{LEE|M}) stays unlinked even once their career is harvested.}

\item{fuzzy_scope}{Optional character vector of \code{source} names. The
name-based passes (\code{birthdate}, \code{loose}) are attempted \strong{only} for
athletes appearing in one of these sources; everything else links on
verified ids alone.

This exists because surname-plus-initial stops identifying anyone once the
corpora are large. Validated against 364 Games swimmers it worked well; run
across 40,000 British and 23,000 international swimmers it merged \verb{Sophie Bateman} with \verb{BATEMAN Sarah}, \verb{Kate Ward} with \verb{WARD Kristy} and \verb{Emilia Vorster} with \verb{VORSTER Eben}. The per-source ambiguity guard cannot catch
those — each key is unique \emph{within} its own source and still refers to two
different people. Scoping to the field you actually need to resolve keeps
the candidate pool small enough for the rule to mean something.}
}
\value{
A \code{data.table}, one row per athlete per source, with \code{key_exact},
\code{key_loose}, \code{key_dob}, \code{person_id} and \code{match_method}. Rows sharing a
\code{person_id} are believed to be the same athlete, so joining two sources is
a self-join on that column.
}
\description{
Every source names the same athlete differently and only some carry a stable
id, so each script that touches two feeds ends up re-deriving its own match.
This builds the mapping \strong{once}, as a table that can be inspected, corrected
by hand and reused — rather than a rule buried in a pipeline.
}
\details{
Athletes are linked in three passes, strongest first, and the pass that
created each link is recorded so downstream users can choose how much to
trust it:
\enumerate{
\item \code{"birthdate"} — same surname and exact date of birth. Two people sharing
both is vanishingly rare, and it is completely immune to name-form
variation. Only fires where a source supplies \code{birthdate}.
\item \code{"exact"} — identical \code{\link[=athlete_key]{athlete_key()}}: the same name tokens in any order.
\item \code{"loose"} — same surname and given initial. This is what recovers
\code{"Thomas DEAN"} / \code{"DEAN Tom"} and \code{"Sam SHORT"} / \code{"SHORT Samuel"}; the
ambiguity guard is what stops it merging twenty different \verb{ZHANG Y}
swimmers.
}

Passes 1 and 2 were checked against each other on the Glasgow 2026 athletics
entry list: of 622 athletes matched by both, they agreed on \strong{100\%}.

Athletes that link to nobody are still returned, with \code{match_method}
\code{"unmatched"} and a \code{person_id} of their own. The table is therefore a
complete registry of every athlete seen in any source, not just the
successful matches — \strong{the unmatched rows are the harvest to-do list}.
}
\seealso{
\code{\link[=athlete_key]{athlete_key()}} for the exact key, \code{\link[=loose_key]{loose_key()}} for the loose one.
}
