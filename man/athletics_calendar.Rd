% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_athletics_calendar.R
\name{athletics_calendar}
\alias{athletics_calendar}
\title{Fetch one page of World Athletics' public event calendar}
\usage{
athletics_calendar(
  start_date = NULL,
  end_date = NULL,
  query = NULL,
  offset = NULL
)
}
\arguments{
\item{start_date, end_date}{\code{Date} or \code{"YYYY-MM-DD"} string, inclusive.
\code{NULL} for no bound on that side. \strong{The endpoint's own filter is not
strictly enforced} -- confirmed 2026-08-30, a 2015-only query returned
at least one 2007 result. It does most of the work; a caller needing a
hard boundary must post-filter \code{start_date} on the returned table.}

\item{query}{Free-text competition name search (substring/fuzzy, as the
site's own search box does), or \code{NULL}.}

\item{offset}{Row offset for pagination, in steps of 100. \code{NULL}/\code{0} for
the first page.}
}
\value{
A \code{data.table} with columns \code{competition_id}, \code{name}, \code{venue},
\code{area}, \code{ranking_category}, \code{disciplines}, \code{competition_group},
\code{competition_subgroup}, \code{start_date}, \code{has_results}, \code{has_api_results},
\code{has_startlist}, and two attributes: \code{"hits"} gives the total match
count for the query (not just this page) -- read it with
\code{attr(result, "hits")} -- and \code{"fetch_ok"} distinguishes a CONFIRMED
zero-hit query (\code{TRUE}, well-formed response, genuinely nothing
matched) from a request that could not be confirmed at all (\code{FALSE}):
a missing/unparseable \verb{__NEXT_DATA__} script tag, most often because a
WAF/bot-detection interstitial returned HTTP 200 with no real payload,
or a truncated/malformed response body. Both failure shapes return the
\emph{same} zero-row table shape as a genuine zero-hit result -- silently
treating them as equivalent was the actual defect found in review
2026-08-30, and this attribute exists to prevent it. Always check
\code{fetch_ok} before trusting a zero-row result, and see
\code{\link[=athletics_calendar_all]{athletics_calendar_all()}}'s \code{"complete"} attribute for the paginated
equivalent.
}
\description{
\code{athletics_find_competition()} and \code{harvest_missing_majors.R}'s discovery
list are built by KEYWORD SEARCH SWEEPS against the wrapper's \verb{/athletes}
route family -- they can only find a competition someone already thought
to search a name for, and on 2026-08-30 that method missed two current
Diamond League legs (Xiamen, Shanghai) that a manual browser check found
by accident. This is a different, better source for discovery specifically
(not results): \code{worldathletics.org/competition/calendar-results} is a
Next.js page whose server-fetched, GraphQL-backed data is embedded
directly in the response HTML as \code{window.__NEXT_DATA__} -- a genuine
date-range-queryable calendar, 39,290 competitions as measured
2026-08-30, spanning recorded history through scheduled future events
(the 2032 Brisbane Olympics is already listed). Confirmed via direct
\code{httr2} GET, no browser/JS execution needed -- the data is present on
the plain server-rendered response.
}
\details{
Competition \code{id} values in the response were confirmed (2026-08-30,
cross-checked against Glasgow 2026 and three Diamond League legs already
in \code{championship_results.rds}) to be the SAME id namespace this package's
\code{athletics_competition_results()} already takes -- a calendar hit's \code{id}
is usable directly as a \code{competition_id} to fetch results.

Pagination is via the \code{offset} query parameter alone, in fixed steps of
100 results; a \code{limit} query parameter was tested (2026-08-30) and is
silently ignored by the endpoint, so it is not exposed here. Passing an
unsupported/invalid parameter combination (an earlier probe combined
\code{limit} with \code{offset}) can make the server-side data fetch fail
silently, returning a page with \code{hits = NULL} and zero results rather
than an HTTP error -- \code{athletics_calendar()} treats that shape as "no
data" rather than crashing, but callers changing the parameter set
should re-verify against a known query before trusting a zero-hit page.
}
\examples{
\dontrun{
athletics_calendar(start_date = "2026-01-01", end_date = "2026-12-31")
}
}
