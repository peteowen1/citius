% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{race_conditions}
\alias{race_conditions}
\title{Magnitude of shared race-condition effects}
\usage{
race_conditions(event_id, calibration = NULL)
}
\arguments{
\item{event_id}{Character vector of canonical event ids.}

\item{calibration}{Optional \code{citius_calibration} from \code{\link[=calibrate]{calibrate()}}.}
}
\value{
Numeric vector of shared-shock standard deviations.
}
\description{
Returns the standard deviation of the \emph{common} shock applied to every athlete
in a race, on the log performance scale. Wind, track surface, temperature,
altitude and pace-setting move an entire field together; pool conditions
barely move a field at all.
}
\details{
The value is \strong{measured}, not assumed. Supply a \code{calibration} from
\code{\link[=calibrate]{calibrate()}} and the shock is the de-biased standard deviation of fitted
race effects for that event. Only the \emph{shared} component belongs here;
athlete-specific variation is carried by \code{sigma} from \code{\link[=estimate_ability]{estimate_ability()}}.

Without a calibration, or for an event with too few observed fields, this
falls back to the registry's \code{cv_prior} — a coarse placeholder that exists
only so the simulator runs before any data has been harvested. Fallback
values are not estimates and should not be treated as such; check
\code{calibration$events$calibrated} to see which events are on real numbers.
}
\seealso{
\code{\link[=calibrate]{calibrate()}}
}
