% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_athletics.R
\name{athlete_results}
\alias{athlete_results}
\title{Fetch an athlete's full World Athletics result history}
\usage{
athlete_results(athlete_id, sex = NULL, birthdate = NULL)
}
\arguments{
\item{athlete_id}{Integer World Athletics athlete id (see \code{\link[=find_athlete]{find_athlete()}}).}

\item{sex}{Optional \code{"M"}/\code{"W"}. Looked up from the profile when \code{NULL}.}

\item{birthdate}{Optional \code{Date}. Looked up from the profile when \code{NULL};
used to compute \code{age} at each performance.}
}
\value{
A \code{data.table} in the canonical result schema — one row per
performance with \code{athlete_id}, \code{date}, \code{event_id}, \code{mark}, \code{perf}, \code{place},
\code{round}, \code{wind}, \code{indoor}, \code{legal}, \code{tier}, \code{age}, \code{venue_country},
\code{result_score}. Zero rows if the athlete has no results.
}
\description{
Returns every recorded performance, not a top-list. This distinction is
critical: ability can be estimated from bests, but \emph{variance} cannot. A
top-list is truncated at the good end, so fitting spread to one would badly
understate how often an athlete has an ordinary day.
}
\details{
The results feed itself carries no sex or date-of-birth, both of which are
needed to resolve canonical events and to place a performance on an aging
curve. They are looked up from the athlete profile unless supplied.
}
\examples{
\dontrun{
athlete_results(14653717)
}
}
