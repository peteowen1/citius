% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_games_crs.R
\name{parse_crs_export}
\alias{parse_crs_export}
\title{Parse a Games Competition Results System export}
\usage{
parse_crs_export(path)
}
\arguments{
\item{path}{Path to the exported JSON.}
}
\value{
A \code{data.table} in the canonical result schema, with swimming extras
\code{reaction_time}, \code{lane} and \code{country}, plus \code{race_key} for \code{\link[=calibrate]{calibrate()}}.
}
\description{
Major multi-sport Games run their results through a Competition Results
System — Glasgow 2026 uses Microplus — which serves a public results app
backed by an authenticated API. The API cannot be reached unattended
(Cloudflare plus a runtime bearer token), but the app renders everything
publicly, so results are captured from the rendered pages and exported as
JSON. This parses that export into the canonical schema.
}
\details{
The export format is deliberately compact: a \code{p} array of pages
(\code{route}, \code{when}, \code{title}) and an \code{r} array of rows referencing pages by
index.

Events the registry does not recognise — relays, Para classifications —
resolve to \code{NA} \code{event_id} rather than being forced onto a nearby event.
They are retained so the export stays faithful to what was scraped; filter
on \code{!is.na(event_id)} before modelling.
}
\examples{
\dontrun{
res <- parse_crs_export("citiusdata/data/glasgow2026_swimming.json")
res[!is.na(event_id) & round == "Final"]
}
}
