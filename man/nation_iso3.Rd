% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nations.R
\name{nation_iso3}
\alias{nation_iso3}
\title{ISO3 code for a canonical nation}
\usage{
nation_iso3(canon)
}
\arguments{
\item{canon}{Character vector from \code{\link[=canonical_nation]{canonical_nation()}}.}
}
\value{
Character vector of ISO3 codes, \code{NA} where unresolved.
}
\description{
Returns \code{NA} where there is no match, never a guess. The function this
replaces fell back to \code{substr(toupper(name), 1, 3)}, which produced 37 codes
shared by more than one nation -- \code{"AUS"} for Australia, Austria \emph{and}
Australasia; \code{"NOR"} for Norway, North Korea, Norfolk Island, North Macedonia
and three more; \code{"CHI"} for Chile and Chinese Taipei. Every one of those
compared a nation's medals against a different country's GDP and
double-counted the denominator.
}
\details{
Historical entities map to a successor economy so that an economic
comparison is possible at all. That is a modelling choice, not a fact: the
Soviet Union's 1980 medals against Russia's population is a statement about
the successor state, and should be reported as such.

Teams that are not economies -- mixed, neutral and refugee teams -- map to
\code{NA} on purpose and are excluded from economic shares rather than attached
to a country.
}
