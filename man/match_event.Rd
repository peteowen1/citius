% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{match_event}
\alias{match_event}
\title{Match source event names onto the canonical registry}
\usage{
match_event(discipline, sex)
}
\arguments{
\item{discipline}{Character vector of source event names.}

\item{sex}{Character vector of sex codes (\code{"M"}/\code{"W"}, or World Aquatics
style \code{"Men"}/\code{"Women"}).}
}
\value{
Character vector of \code{event_id}s, \code{NA} where unmatched.
}
\description{
Data feeds spell events inconsistently (\code{"10000 Metres"} vs \code{"10,000 Metres"}, \code{"Men's 100m Freestyle"} vs \code{"100m Freestyle"}). This resolves a
source string plus a sex code to a canonical \code{event_id}.
}
\details{
Unmatched names return \code{NA} rather than guessing. Silently mapping an
unrecognised event onto a plausible-looking neighbour would corrupt an
athlete's history in a way that is very hard to detect downstream.
}
\examples{
match_event(c("100 Metres", "Men's 100m Freestyle"), c("M", "M"))
}
