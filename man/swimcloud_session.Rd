% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_swimcloud.R
\name{swimcloud_session}
\alias{swimcloud_session}
\title{Open a SwimCloud browser session}
\usage{
swimcloud_session(port = 9333L, wait = 7)
}
\arguments{
\item{port}{DevTools port to listen on.}

\item{wait}{Seconds to allow the bot-management interstitial to clear. The
first page load is challenged; subsequent requests in the session are not.}
}
\value{
A list with the \code{chromote} session and a \code{close()} function. Always
close it — an orphaned Chrome keeps running otherwise.
}
\description{
SwimCloud is fronted by a bot-management layer that returns \strong{403 to every
non-browser client} — \code{httr2}, \code{curl} and headless Chrome alike (headless
advertises \code{HeadlessChrome} in its user agent and is served a challenge page).
A real Chrome, driven over the DevTools protocol, is served normally.
}
\details{
So this launches an actual Chrome and attaches to it. The browser is \emph{real};
only the driving is automated. No attempt is made to disguise the client —
if a future change blocks script-driven browsers too, that is a decision to
respect rather than route around.

An isolated \code{--user-data-dir} is used so the session cannot read or disturb
the user's own Chrome profile, cookies or logins.
}
