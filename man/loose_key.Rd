% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{loose_key}
\alias{loose_key}
\title{Loose athlete key: surname plus given initial}
\usage{
loose_key(x, name_order = c("surname_first", "given_first"))
}
\arguments{
\item{x}{Character vector of names.}

\item{name_order}{Fallback when a name carries no case signal: one of
\code{"surname_first"}, \code{"given_first"}. Used only for all-caps or all-lower
names.}
}
\value{
Character vector of keys, \code{NA} where no given name is present.
}
\description{
\code{\link[=athlete_key]{athlete_key()}} is exact, and exactness costs real matches: the same swimmer
is \code{"Sam SHORT"} at the Commonwealth Games and \code{"SHORT Samuel"} at World
Aquatics, so their two careers never join. Short and full forms appear in
\emph{both} directions (\code{"Joshua LIENDO"} / \code{"LIENDO Josh"}), so no rule that
prefers the longer or the shorter form can work. Keying on surname plus the
first initial matches all of them.
}
\details{
This key is \strong{not safe on its own}. Measured on the swimming corpus it is
ambiguous for 4.85\% of keys, concentrated exactly where you would expect:
\code{ZHANG|Y} covers \strong{20 different swimmers}. Always drop keys that are
ambiguous within a source before joining on it — which is what
\code{\link[=athlete_crosswalk]{athlete_crosswalk()}} does.
}
\examples{
loose_key("Sam SHORT", "given_first")
loose_key("SHORT Samuel", "surname_first")   # identical
}
