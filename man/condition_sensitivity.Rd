% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{condition_sensitivity}
\alias{condition_sensitivity}
\title{Athlete-specific sensitivity to shared race conditions}
\usage{
condition_sensitivity(ability, event_id, calibration = NULL)
}
\arguments{
\item{ability}{A \code{data.table} of entrants as passed to \code{\link[=simulate_event]{simulate_event()}}.}

\item{event_id}{Canonical event id for the race.}

\item{calibration}{Optional \code{citius_calibration} from \code{\link[=calibrate]{calibrate()}}. Without
one, every athlete is assigned a sensitivity of 1, which makes the shock a
pure main effect that cannot reorder the field.}
}
\value{
Numeric vector of multipliers, one per row of \code{ability}, in the same
order. Rescaled so the field mean is 1, preserving the magnitude
\code{\link[=race_conditions]{race_conditions()}} intends.
}
\description{
Returns a multiplier per athlete describing how strongly each is moved by the
shared condition shock drawn in \code{\link[=simulate_event]{simulate_event()}}. A value of \code{1} means the
athlete is affected exactly as much as the field average; \code{1.5} means half
again as much; \code{0} means unaffected.
}
\details{
This function is what makes conditions capable of changing \emph{placings}. The
shared shock on its own is a main effect and cancels out of every pairwise
comparison, so it can only move absolute marks. Sensitivity turns it into an
interaction: if athletes respond differently to a headwind, a cold pool or a
slow pace, then conditions genuinely reorder the field.
}
\seealso{
\code{\link[=calibrate]{calibrate()}}
}
