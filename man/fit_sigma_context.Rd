% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{fit_sigma_context}
\alias{fit_sigma_context}
\title{Measure how far championship spread departs from pooled spread}
\usage{
fit_sigma_context(results, min_history = 4L, min_n = 500L)
}
\arguments{
\item{results}{Canonical results with \code{perf}, \code{athlete_id}, \code{event_id},
\code{tier} and \code{round}.}

\item{min_history}{Minimum marks an athlete-event needs to contribute; below
this the within-athlete spread is mostly estimation noise.}

\item{min_n}{Minimum championship marks for a family to be estimated. Families
below it get a ratio of 1, leaving \code{sigma} untouched.}
}
\value{
A \code{data.table} of \code{family}, \code{ratio}, \code{sigma_pooled}, \code{sigma_champ}
and the \code{n} behind each.
}
\description{
\code{sigma_within} is fitted across an athlete's whole history, but a forecast
targets a top-tier final. Those are not the same distribution. A championship
throw final holds implement, circle and officiating far more constant than the
corpus average, so the pooled spread is too wide for it; road running inverts,
because a championship marathon is tactical while most corpus road races are
paced time-trials.
}
\details{
The ratio is the correction, measured rather than assumed. Against the
standardised backtest error it lines up closely — throw 0.681 against a
measured \code{sd(z)} of 0.698, road 1.141 against 1.142, correlation 0.80 across
eight families.

Fitted per family rather than per event: an event-level ratio is a variance
ratio on a few thousand marks, and variance estimates are far noisier than
means at the same sample size.
}
\seealso{
\code{\link[=estimate_ability]{estimate_ability()}}, which applies this to the sigma it returns.
}
