% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/athlete_match.R
\name{athlete_key}
\alias{athlete_key}
\title{Order-invariant athlete name key}
\usage{
athlete_key(x)
}
\arguments{
\item{x}{Character vector of athlete names.}
}
\value{
Character vector of keys, safe to join on.
}
\description{
Feeds disagree about name order. World Aquatics writes \code{"SJOESTROEM Sarah"};
the Commonwealth Games results system writes \code{"Hannah STERRY"}. Concatenating
the letters gives \code{SJOESTROEMSARAH} and \code{HANNAHSTERRY}, so a straight key
matched \strong{1 of 388} Glasgow swimmers to their own history — which is why no
swimming predictions existed for that meet.
}
\details{
Sorting the name \emph{tokens} before joining makes the key order-invariant and
lifts that to 46\%. It stays \strong{exact}: two names match only if they contain
precisely the same parts. That matters more here than a higher hit rate would
— the same reasoning as \code{\link[=match_event]{match_event()}} returning \code{NA} rather than guessing.
A wrongly linked athlete silently merges two people's careers into one
ability estimate, and nothing downstream can detect it.

What it deliberately does \strong{not} do: fuzzy or phonetic matching. Diacritic
transliterations (\code{Sjöström} / \code{SJOESTROEM}) and abbreviated middle names stay
unmatched rather than being guessed at.
}
\examples{
athlete_key(c("Hannah STERRY", "STERRY Hannah"))   # identical
athlete_key("Mohamed Aan HUSSAIN") == athlete_key("HUSSAIN Mohamed Aan")
}
