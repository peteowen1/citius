% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{fit_numeric_effect}
\alias{fit_numeric_effect}
\title{Estimate the effect of a continuous covariate on marks}
\usage{
fit_numeric_effect(results, covariate, by_event = TRUE, min_n = 200L, trim = 5)
}
\arguments{
\item{results}{Canonical results.}

\item{covariate}{Numeric column name.}

\item{by_event}{Fit a separate slope per event.}

\item{min_n}{Minimum marks for a slope to be fitted.}

\item{trim}{Ignore values beyond this many SDs from the covariate mean;
extreme values are usually recording errors and would lever the slope.}
}
\value{
A \code{data.table} of \code{event_id}, \code{beta}, \code{n} and \code{r2}.
}
\description{
The continuous counterpart to \code{\link[=fit_context_effect]{fit_context_effect()}}: fits a slope rather
than per-level means. \code{\link[=fit_wind_effect]{fit_wind_effect()}} is a specialisation of this for
wind; use this for anything else measured on a scale, such as swimming
reaction time.
}
\details{
Fitted per event by default, because the same covariate can matter very
differently across events — wind moves a 100m far more than a 200m, where
half the race is on the bend.

\strong{Note on predictive use.} Some covariates are only known \emph{after} the race.
Reaction time is the clear case: you cannot know a future one. Such covariates
still earn their place by cleaning historical marks — a swimmer who botched
one start should not carry that in their ability estimate — but they cannot
be supplied for a race yet to happen. The athlete's typical value is already
inside their ability.
}
