% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_athletics_official_profile.R
\name{athletics_athlete_official_profile}
\alias{athletics_athlete_official_profile}
\title{Fetch an athlete's OFFICIAL biographical data from worldathletics.org}
\usage{
athletics_athlete_official_profile(athlete_id)
}
\arguments{
\item{athlete_id}{Integer World Athletics athlete id (the same
\code{athlete_id}/\code{aaId} used throughout this package and the corpus).}
}
\value{
A one-row \code{data.table} with \code{athlete_id}, \code{family_name},
\code{given_name}, \code{country_code}, \code{country_name}, \code{birthdate}, \code{sex}
(\code{"M"}/\code{"W"}, from the \code{male} boolean), \code{iaaf_id} (a distinct secondary
id some older records key on). Zero rows if the athlete id does not
resolve -- but check attribute \code{"fetch_ok"} before treating a zero-row
result as "this id doesn't exist": \code{fetch_ok = TRUE} means a
well-formed page confirmed the id is unresolved; \code{fetch_ok = FALSE}
means the request itself couldn't be confirmed at all (a missing/
unparseable \verb{__NEXT_DATA__} node, most often a WAF/bot-detection
interstitial returning HTTP 200 with no real payload) -- the two used
to return an identical shape, which is exactly wrong for a function
whose caller is realistically a batch resolving many ids in a loop: a
mid-run block would have silently read every subsequent athlete as
"does not exist." Found in review 2026-08-30.
}
\description{
\code{\link[=athletics_athlete_profile]{athletics_athlete_profile()}} reads the community wrapper's
(\code{worldathletics.nimarion.de}) \verb{/athletes/\{id\}} route. This is a
different, first-party source: \verb{worldathletics.org/athletes/-/-\{id\}}
(the country/name-slug segments in the URL are cosmetic -- confirmed
2026-08-30 that \verb{-/-} in their place resolves identically to the real
slug) embeds a Next.js \verb{__NEXT_DATA__} payload with the athlete's
registered name split into \code{familyName}/\code{givenName}, exactly as World
Athletics itself records it.
}
\details{
Built to close a real bug: a name-string sanity check this session
searched literally for \code{"Mondo Duplantis"} and \code{"Sydney McLaughlin"} and
found neither, because those are not the athletes' registered names --
the corpus (and this endpoint) has \code{"Armand Duplantis"} (Mondo is a
nickname) and \code{"Sydney Mclaughlin"} (lowercase second syllable). Looking
athletes up by \code{athlete_id} through this function sidesteps name-string
guessing entirely.
}
\examples{
\dontrun{
athletics_athlete_official_profile(14679502)
}
}
