% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{citius-deprecated}
\alias{citius-deprecated}
\alias{competition_results}
\alias{athlete_results}
\alias{athlete_profile}
\alias{find_athlete}
\alias{find_competition}
\alias{harvest_competitions}
\title{Deprecated unprefixed athletics functions}
\usage{
competition_results(...)

athlete_results(...)

athlete_profile(...)

find_athlete(...)

find_competition(...)

harvest_competitions(...)
}
\arguments{
\item{...}{Passed through to the replacement.}
}
\value{
Whatever the replacement returns.
}
\description{
The feed adapters are now prefixed by the federation they call, so a reader
can tell from the name which source a function talks to:
}
\details{
\tabular{ll}{
   was \tab now \cr
   \code{competition_results()} \tab \code{\link[=athletics_competition_results]{athletics_competition_results()}} \cr
   \code{athlete_results()} \tab \code{\link[=athletics_athlete_results]{athletics_athlete_results()}} \cr
   \code{athlete_profile()} \tab \code{\link[=athletics_athlete_profile]{athletics_athlete_profile()}} \cr
   \code{find_athlete()} \tab \code{\link[=athletics_find_athlete]{athletics_find_athlete()}} \cr
   \code{find_competition()} \tab \code{\link[=athletics_find_competition]{athletics_find_competition()}} \cr
   \code{harvest_competitions()} \tab \code{\link[=athletics_harvest_competitions]{athletics_harvest_competitions()}} \cr
}


The swimming adapters were already \verb{aquatics_*}, so \code{competition_results()}
read as though it were sport-agnostic when it only ever called World
Athletics. Prefixing by \strong{federation} rather than by sport is deliberate:
World Aquatics governs six sports, so \code{swimming_results()} would be wrong the
moment diving is added, while \code{aquatics_results()} stays correct.

These shims warn once per session and will be removed. They exist so a
downstream consumer mid-build does not break on the rename.
}
