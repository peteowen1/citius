% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbour_combine.R
\name{fit_neighbour_links}
\alias{fit_neighbour_links}
\title{Measure how much one event's ability says about another's}
\usage{
fit_neighbour_links(
  ability,
  target_events,
  neighbour_events = NULL,
  elite_quantile = 0.9,
  min_n = 30L
)
}
\arguments{
\item{ability}{An \code{\link[=estimate_ability]{estimate_ability()}} table spanning target and neighbour
events, for the whole rated population rather than one race's entrants --
the offsets and correlations are population quantities.}

\item{target_events}{Events to fit links INTO.}

\item{neighbour_events}{Candidate events to fit links FROM. Defaults to every
other event present in \code{ability}; pairs that fail \code{min_n} are dropped, so an
unrelated event costs nothing but is not silently kept either.}

\item{elite_quantile}{Quantile of target \code{ability_raw} defining "elite".}

\item{min_n}{Minimum athletes with both ratings for a pair to be fitted.}
}
\value{
A \code{data.table} with \code{event_id}, \code{neighbour_event_id}, \code{n}, \code{delta},
\code{r}, \code{sigma2_target} and \code{tau2}, suitable as \code{links} for
\code{\link[=combine_neighbour_ability]{combine_neighbour_ability()}}.
}
\description{
Fits, per (target, neighbour) pair, the three quantities
\code{\link[=combine_neighbour_ability]{combine_neighbour_ability()}} needs: the fixed offset between the two events
on the oriented log scale, how strongly they correlate, and how far athletes
sit off that conversion. Nothing is hand-set.
}
\details{
Fitted on the ELITE subset of the target event, because that is the
population a championship final is drawn from and the relationship is not the
same lower down: measured 2026-09-14 on men's 1500m -> 5000m, the OLS slope
runs 0.799 across all 10,629 men with both ratings and 0.196 across the top
decile. Using the all-athlete relationship on an elite field would transfer
far too much.

\strong{\code{tau2} deliberately does not subtract estimation noise.} The textbook form
would be \code{var(ability) - mean(ability_se^2)}, but \code{ability_se} is as large as
the entire between-athlete spread at the elite tail, so that subtraction
returns ~0 and every neighbour then counts as a perfect proxy -- measured, it
let a four-year-old 800m rating take 28\% of Ingebrigtsen's 5000m weight. The
observed spread is conservative (tau too large, neighbours under-weighted)
and never degenerate, which is the right way round for a term that decides
how much a different event gets to speak.
}
\seealso{
\code{\link[=combine_neighbour_ability]{combine_neighbour_ability()}}
}
