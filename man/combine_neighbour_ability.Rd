% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbour_combine.R
\name{combine_neighbour_ability}
\alias{combine_neighbour_ability}
\title{Combine an athlete's neighbouring-event ratings into their own}
\usage{
combine_neighbour_ability(ability, links = NULL)
}
\arguments{
\item{ability}{An \code{\link[=estimate_ability]{estimate_ability()}} table. Must contain the neighbour
events' rows for these athletes, not only the target event's.}

\item{links}{Output of \code{\link[=fit_neighbour_links]{fit_neighbour_links()}}. \code{NULL} is a true no-op.}
}
\value{
\code{ability} with \code{ability_raw} and \code{ability} updated for rows that had
at least one usable neighbour; every other row untouched.
}
\description{
\code{\link[=estimate_ability]{estimate_ability()}} groups everything \code{by = .(athlete_id, event_id)}, so a
man's 5000m rating never sees his 1500m marks. Ingebrigtsen won the 2026
Budapest 5000m in 12:59.24 rated last of twelve, predicted 13:14, because his
only 5000m races were tactical championship finals while three fast Diamond
League 1500ms in the previous month were invisible to that card.
}
\details{
The athlete's own rating stays the baseline. Every neighbour event they are
rated in also speaks, weighted by two things and nothing else:
\enumerate{
\item \strong{how much we know that rating} -- its evidence variance \code{sigma^2 / w_total}
\item \strong{how much that event says about this one} -- \code{tau2} from
\code{\link[=fit_neighbour_links]{fit_neighbour_links()}}, which is \code{(1 - r^2)} of the target's own spread
}

combined as one inverse-variance weight, \code{1 / (sigma^2/w_total + tau2)}. An
event we know little about is down-weighted; an event that says nothing about
the target (\code{r -> 0}) has \code{tau2} approaching the target's whole spread and
earns almost no weight, so an unrelated pair costs nothing and needs no
hand-maintained list of which events may talk to which.

\strong{Confidence is \code{w_total / sigma^2}, NOT \code{1 / ability_se^2}, and the
difference is not cosmetic.} \code{ability_se} is \code{sigma / sqrt(w_total + kappa)}
and is bounded by each event's own \code{sigma_between}, so it is not comparable
across events at all; worse, \code{temper_unevidenced()} overwrites it with the
event median for anyone under \code{w_total = 0.05}, which is correct for widening
that athlete's own simulated spread and actively wrong for deciding how far
to trust one rating against another. Measured on Ingebrigtsen 2026-09-14: an
\code{ability_se} weighting gave his 2022 800m (w_total 0.0128) \strong{29\%} of his
weight against his own 5000m's 23\%; the evidence weighting gives it \strong{1\%}.

Only \code{ability_raw} and \code{ability} move. \code{shrinkage}, \code{sigma} and \code{w_total} are
left alone: this adds evidence about where the athlete's ability sits, it
does not re-argue how confident the estimator should have been.
}
\seealso{
\code{\link[=fit_neighbour_links]{fit_neighbour_links()}}, \code{\link[=estimate_ability]{estimate_ability()}}
}
