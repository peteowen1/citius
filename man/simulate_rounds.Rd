% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounds.R
\name{simulate_rounds}
\alias{simulate_rounds}
\title{Simulate a championship through its rounds}
\usage{
simulate_rounds(
  ability,
  structure,
  n_sims = 10000L,
  calibration = NULL,
  seed = NULL
)
}
\arguments{
\item{ability}{Entrants, as returned by \code{\link[=estimate_ability]{estimate_ability()}}.}

\item{structure}{A list of rounds, each a list with \code{races} (how many races
in the round), \code{advance} (automatic qualifiers per race) and optionally
\code{fastest_losers} (further qualifiers on time across the round). The last
round is the final and needs only \code{races = 1}.}

\item{n_sims}{Simulations.}

\item{calibration}{Optional \code{citius_calibration}.}

\item{seed}{Optional integer seed.}
}
\value{
A \code{data.table} with one row per athlete: \verb{p_reach_*} for each round
after the first, plus \code{p_gold}, \code{p_medal} and \code{p_final}.
}
\description{
\code{\link[=simulate_event]{simulate_event()}} answers "who wins this race, given the field". A
championship asks something harder: the final field is not known, because
athletes must first survive heats and semi-finals. Medal probabilities that
condition on a known final overstate every contender, since none of them is
certain to be there.
}
\section{What is drawn once and what is drawn per race}{

This is the modelling decision that matters, and it is not the same for each
term:
\itemize{
\item \code{ability_se} (how little we know the athlete) and \code{form_sd} (their form
this week) are drawn \strong{once per championship}. They describe the athlete
over the meet, so an athlete who is below par is below par in the heat and
again in the final. Redrawing them per round would let a bad heat be
forgotten by the final and make qualification far too forgiving.
\item \code{sigma} (race-to-race execution) and the shared race shock are drawn
\strong{per race}, because they are properties of the race.
}

Getting this backwards is the easy mistake: it makes progression look like
independent coin flips and understates how strongly a heat predicts a final.
}

\section{Lane allocation}{

Athletes are snake-seeded on ability, which is what championships approximate
when they seed on season's best — the strongest are spread across heats
rather than drawn together. Random allocation would be materially different:
it lets two contenders collide in a heat and eliminate one, inflating upsets.
}

\examples{
\dontrun{
simulate_rounds(entrants, structure = list(
  list(races = 3, advance = 2, fastest_losers = 2),
  list(races = 1)))
}
}
