% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{race_shock}
\alias{race_shock}
\title{Race shock: the shrunk field-mean surprise}
\usage{
race_shock(resid, var_race, var_resid, cap_sd = 3)
}
\arguments{
\item{resid}{Cleaned perf minus expected perf, one per athlete in the race.
\code{NA} (no expectation, e.g. a debutant) is excluded from the mean.}

\item{var_race, var_resid}{Race-shock and residual variances from
\code{\link[=conditions_params]{conditions_params()}}. \code{var_resid} should be the variance of the
expectation actually used — forecast error for a live forecast.}

\item{cap_sd}{Winsorising cap in residual standard deviations.}
}
\value{
A single number in perf-log units (positive = fast race); \code{0} when
no athlete has an expectation.
}
\description{
What the whole field shared on the day after wind, altitude and indoor are
removed and each athlete's own expected level is subtracted. It is the BLUP
of a one-way random effect with athlete levels treated as known:
\code{mean(resid) * n * var_race / (n * var_race + var_resid)}, so a small or
noisy field is pulled towards zero. Verified against \code{lme4}'s race BLUP on
the men's 100m fitting sample: correlation 1.0000, RMSE 3e-6.
}
\details{
Residuals are winsorised at \code{cap_sd} residual standard deviations first.
Without that, one athlete who pulls up (12.47 against an expected 10.55 in a
real 100m heat) reads as a "slow race" and credits the rest of the field
0.18s they did not earn. The cap keeps the shrinkage arithmetic intact and
bounds any single athlete's leverage.
}
