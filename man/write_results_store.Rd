% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{write_results_store}
\alias{write_results_store}
\title{Write a result corpus as a partitioned parquet dataset}
\usage{
write_results_store(results, path, partition_by = "event_id")
}
\arguments{
\item{results}{A results \code{data.table}.}

\item{path}{Destination directory. Overwritten.}

\item{partition_by}{Column to partition on. \code{event_id} unless you have
measured a better one for your access pattern.}
}
\value{
\code{path}, invisibly.
}
\description{
\code{.rds} forces the whole corpus into memory before a single row can be
filtered. At the 300k rows this project started with that was merely wasteful;
at the ~8.5M rows the career harvests produce it is the dominant cost, and it
has already OOM-killed a backtest once.
}
\details{
Measured on 8.6M rows, running the query the backtest actually issues
("these events, this date window, these 11 columns"):\tabular{lll}{
   store \tab per query \tab across 825 backtest meets \cr
   \code{.rds} \tab 46.1s \tab ~10.6 hours \cr
   parquet \tab 8.7s \tab 120 min \cr
   \strong{parquet partitioned by \code{event_id}} \tab \strong{0.39s} \tab \strong{5 min} \cr
}


Partitioning wins because every query in this pipeline filters on \code{event_id},
so all but a handful of files are skipped without being opened. It costs
roughly 2x the disk (586 MB against 273 MB) since many small files compress
worse than one large one — an easy trade for 118x.
}
\examples{
\dontrun{
write_results_store(champs, "citiusdata/data/athletics_store")
}
}
