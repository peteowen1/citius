% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_athletics_calendar_results.R
\name{athletics_calendar_results}
\alias{athletics_calendar_results}
\title{Fetch a competition's full results directly from worldathletics.org}
\usage{
athletics_calendar_results(competition_id)
}
\arguments{
\item{competition_id}{Integer World Athletics competition id -- the same
namespace as \code{\link[=athletics_calendar]{athletics_calendar()}}'s \code{id} and this package's
\code{athletics_competition_results()}.}
}
\value{
A \code{data.table}, one row per result, with \code{competition_id},
\code{comp_name}, \code{venue}, \code{comp_start}, \code{comp_end}, \code{ranking_category},
\code{event_name}, \code{event_id}, \code{sex_code} (\code{"M"}/\code{"W"}, from the response's
\code{gender}), \code{is_relay}, \code{round} (the response's \code{race} field, e.g.
\code{"Final"}), \code{race_id}, \code{race_number}, \code{race_date} (frequently \code{NA} --
see Details), \code{athlete_id} (parsed from the competitor's \code{urlSlug},
e.g. \code{"ukraine/mykhaylo-kokhan-14686964"} -> \code{14686964}; \code{NA} if the
slug doesn't end in a parseable id), \code{athlete_name}, \code{athlete_iaaf_id},
\code{birthdate}, \code{nationality}, \code{mark}, \code{place}, \code{points}, \code{wind},
\code{records}, \code{remark}. Attribute \code{"fetch_ok"} follows the same
confirmed-vs-unconfirmed discipline as the other two functions in this
family: \code{TRUE} for a well-formed response (whether or not it has
results -- e.g. a competition with \code{hasResults = FALSE} on the
calendar can render a page with empty \code{eventTitles}), \code{FALSE} when the
fetch itself could not be confirmed (missing/unparseable
\verb{__NEXT_DATA__}, most often a WAF/bot-detection interstitial returning
HTTP 200 with no real payload). A definitive 404 from
\code{\link[=citius_get_html]{citius_get_html()}} is \code{fetch_ok = TRUE} (a confirmed answer -- the
competition id doesn't resolve here), matching the fix already applied
to the other two functions in this family.
}
\description{
A third source in the same family as \code{\link[=athletics_calendar]{athletics_calendar()}} and
\code{\link[=athletics_athlete_official_profile]{athletics_athlete_official_profile()}}: \code{worldathletics.org/competition/ calendar-results/results/{competition_id}} is a Next.js page whose
server-fetched data is embedded in the response HTML as
\code{window.__NEXT_DATA__}, fetchable via a plain GET, no browser needed.
}
\details{
Built to solve a real, concrete problem: on 2026-08-30, 26 competitions
(15 historic Diamond League editions plus 11 other T1/T2 meets) were
found to consistently fail with HTTP 500 from the community wrapper
(\code{worldathletics.nimarion.de}, what \code{\link[=athletics_competition_results]{athletics_competition_results()}}
uses) -- confirmed NOT a stale-id problem (World Athletics' own site
resolves every one of them at the SAME id, with \code{hasResults = TRUE}), so
the wrapper itself has a bug specific to these competitions. This
function fetches from a different first-party surface entirely, verified
2026-08-30 to return real, complete results for at least one of the 26
(competition 7214476, 17 events).

The response nests four levels deep: competition -> \code{eventTitles} ->
\code{events} -> \code{races} -> \code{results}. Flattened here into one row per result.
}
\section{Known schema gap -- NOT a drop-in replacement for \code{championship_results.rds}}{

This endpoint's fields do not map cleanly onto \code{CITIUS_DB_SCHEMA $championship_results} (\code{R/db_schema.R}). What's genuinely absent here
and would need separate derivation before any merge into the corpus:
\code{race_key} (no equivalent field -- would need constructing from
\code{competition_id}/\code{event_id}/\code{race_id}, and this endpoint's \code{race_id}
appears to be a small shared code across competitions, e.g. \code{10} for a
plain final, not competition-unique -- verify uniqueness before using it
as a key component), \code{discipline}/\code{discipline_code} (only a free-text
\code{event_name} like \code{"Men's Hammer Throw"} is given, mixing discipline and
sex into one string), \code{venue_country}/\code{venue_city}/\code{venue_stadium} (only
one free-text \code{venue} string, e.g. \code{"Zdzisław Krzyszkowiak Stadium, Bydgoszcz (POL)"}), \code{is_technical}/\code{legal}/\code{tier}/\code{orientation}/\code{perf}
(all derived/computed columns in this package's model, not present on
any raw source), \code{age} (would need \code{birthdate} combined with \code{race_date},
which is frequently \code{NULL} in the raw response -- confirmed on
competition 7214476, only \code{comp_start}/\code{comp_end} are reliably
populated), \code{value_raw}/\code{mark_string} (only a single already-formatted
\code{mark} string is given, e.g. \code{"79.84"}, not the raw/display split this
package's schema expects), \code{indoor}, \code{comp_day}, \code{birthdate_year_only}.
Using this function's output to backfill the corpus needs a real mapping
layer, not attempted here.
}

\examples{
\dontrun{
athletics_calendar_results(7214476)
}
}
